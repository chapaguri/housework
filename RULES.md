# 집안일 규칙

앱: https://chapaguri.github.io/housework/

3주를 한 사이클로 돌린다. 매일 체크하고, 사이클이 끝나면 정산한다.

---

## 1. 체크 규칙

### 실행과 확인

항목마다 버튼이 두 개다.

| 버튼 | 누르는 사람 | 동작 |
|---|---|---|
| **실행** (사각형) | 담당자 | 누를 때마다 `완료 → 미이행 → 취소` 순환 |
| **확인** (원형) | 상대방 | 눌러서 확인 표시 / 다시 눌러 해제 |

확인은 **금액 계산에 들어가지 않는다.** 서로 확인했다는 표시일 뿐이다.

### 미이행 처리

지난 날짜인데 실행 체크가 없으면 **자동으로 미이행**으로 집계된다.
오늘과 미래 날짜는 아직 미이행으로 세지 않는다.

### 제외일

여행·외박 등으로 집을 비운 날은 날짜 옆 **제외** 버튼을 누른다.

- 그날은 이행·미이행 어느 쪽으로도 세지 않는다
- 일일 완주 보너스 대상도 아니다
- 그 주의 **주간 횟수 목표가 비례 축소**된다 (2일 제외 → 로봇 청소기 주 3회가 2회로)

---

## 2. 항목 주기

| 주기 | 나타나는 날 |
|---|---|
| **매일** | 월~일 전부. 원본 시트의 '평일' 열이 실제로는 매일이었다 |
| **주말** | 토·일 |
| **화,목,일** 같은 요일 지정 | 지정한 요일만 |
| **주N회** | 요일과 무관. 그 주 안에 N번 채우면 된다 |
| **매달** | 3주차에만 표시. 주말에 하는 것을 기준으로 한다 |

주간 횟수 항목(주N회)은 정산 화면에 `수건 1/1` `로봇 3/3` 처럼 칩으로 보인다.
**참고용이고 일일 완주 판정에는 넣지 않는다** — 넣으면 매일 빨래를 해야 완주가 되어버린다.

---

## 3. 용돈 정산

### 기본

**매달 500,000원.** 앱이 계산에 더하지 않고 안내로만 표시한다.
사이클은 3주라서 달 경계와 맞지 않기 때문이다.

### 보너스

| 조건 | 금액 |
|---|---|
| 하루 예정 항목을 **전부 이행** | **+10,000원** |
| 추가 청소 **1건** | **+10,000원** |

**일일 완주**는 그날 예정된 항목이 전부 `완료`여야 한다. 하나라도 미이행이거나
체크가 비어 있으면 보너스가 없다. 제외일은 대상이 아니다.

**추가 청소**로 인정되는 항목 (각각 월 1회):

- 이불 세탁
- 창틀 청소
- 공청기 필터 청소

그 외 매달 항목(화장실 전체 청소, 냉장고·음쓰 정리, 집안 전체 먼지 제거)은
기본 루틴이라 보너스가 붙지 않는다.

### 그 외 (합의 사항)

- 주 내에 여행이 있으면, 출발날 &amp; 도착날 합산하여 청소 진행 시 **20,000원**
- 2주에 걸쳐 여행하면 합의하여 청소하고 **각 10,000원씩**
- 그 외 특이사항은 두 사람 합의로 조절 가능
- 단, **집안일 미루고 용돈 획득하기는 불가**

---

## 4. 사이클

- 한 사이클은 **3주(21일)** 다
- 시작일은 요일과 무관하게 아무 날짜나 고를 수 있다
- 날짜를 고른 뒤 **시작하기**를 눌러야 등록된다. 고르기만 해서는 등록되지 않는다
- **기존 사이클과 기간이 겹치면 등록되지 않는다.** 같은 날이 두 사이클에 들어가면
  집계가 이중으로 잡히기 때문이다
- 사이클을 꼭 이어서 시작할 필요는 없다. 비는 기간이 있어도 된다

### 정산 화면

사이클을 **진행·대기 / 종료** 두 그룹으로 나눠 보여준다.

| 상태 | 조건 |
|---|---|
| 진행 중 | 오늘이 3주 기간 안에 있음 |
| 대기 | 시작일이 아직 오지 않음 |
| 종료 | 3주가 지났거나, 3주 전부 정산 마감됨 |

정산 카드를 **탭하면** 그 사이클을 체크 탭에서 열어볼 수 있다. 지난 사이클도 수정 가능하다.

### 주차 마감

`정산완료`를 누르면 그 주차가 **마감**되어 실행·확인·제외·담당자를 모두 수정할 수 없다.
다시 눌러 마감을 해제할 수 있다.

---

## 5. 담당자

주차별로 이름을 입력한다. 로테이션은 수동이다.

**이름을 바꿔도 데이터는 꼬이지 않는다.** 체크 기록은 담당자와 연결돼 있지 않고,
누가 눌렀는지는 아예 기록하지 않는다.

다만 과거 주차의 이름을 바꾸면 그 주 정산의 담당자 표기도 함께 바뀐다.
개명·오타 수정이면 그대로 두고, 담당자 교체라면 앞으로의 주차만 바꾸는 것이 맞다.
이미 마감된 주차는 입력이 잠겨 있다.

---

## 6. 항목 관리

항목은 구글 시트 **`항목` 탭**에서 관리한다. 앱 수정이나 배포가 필요 없고,
저장하면 20초 안에 두 사람 화면에 반영된다.

앱의 **⚙ 설정 → 구글 시트 열기** 로 바로 갈 수 있다.

### 열 구성

| 열 | 이름 | 설명 |
|---|---|---|
| A | **ID** | **절대 바꾸지 말 것** |
| B | 구분 | `화장실` `주방` `거실` `세탁` `청소` 중 하나 |
| C | 항목명 | 자유롭게 변경 가능 |
| D | 부가설명 | 작은 글씨로 함께 표시 |
| E | 주기 | `매일` `주말` `매달` `주3회` 또는 `화,목,일` |
| F | 목표 | `주N회` 일 때의 횟수 |
| G | 보너스 | `Y` 면 추가 청소 보너스 대상 (매달 항목만) |
| H | 적용시작일 | `2026-08-18` 형식. 이 날짜부터 나타난다 |
| I | 사용 | `N` 이면 앞으로 표시하지 않는다 |
| J | 짧은이름 | 정산 화면 주간 목표 칩에 쓰는 축약 이름 |

**행 순서가 화면 표시 순서다.** 같은 구분끼리 묶어두면 보기 좋다.

### 지켜야 할 세 가지

**① ID는 절대 바꾸지 않는다**

과거 체크가 `d|2026-08-17|k1` 처럼 ID로 묶여 있다. ID를 바꾸면 그 항목의 지난
기록이 전부 미아가 된다. 반면 **항목명은 표시용일 뿐이라 마음대로 바꿔도 안전하다.**

**② 없앨 항목은 삭제하지 말고 `사용 = N`**

행을 지우면 화면에서 사라지는 것은 물론 **과거 정산 숫자까지 달라진다.**
`N`으로 두면 앞으로만 안 보이고 지난 기록은 그대로 보존된다.

**③ 새 항목에는 `적용시작일`을 넣는다**

넣지 않으면 **과거 날짜에도 소급 적용되어 미이행이 무더기로 생긴다.**
오늘부터 적용하려면 오늘 날짜를 넣으면 된다.

### 안전장치

시트를 읽지 못하거나 형식이 맞지 않으면 앱에 내장된 **기본 목록으로 자동 폴백**한다.
오타 하나로 앱이 깨지지는 않는다. 현재 어느 쪽으로 동작 중인지는
**⚙ 설정 → 항목 설정** 에서 확인할 수 있다.

잘못된 행은 그 행만 건너뛴다. ID가 중복되면 첫 행만 인정한다.

---

## 7. 데이터

체크 기록은 구글 시트 **`앱데이터` 탭**에 key/value 로 쌓인다.

| 키 | 뜻 | 값 |
|---|---|---|
| `d\|2026-08-17\|k1` | 실행 | `1` 완료 / `2` 미이행 |
| `c\|2026-08-17\|k1` | 확인 | `1` |
| `x\|2026-08-16` | 제외일 | `1` |
| `m\|2026-08\|m3` | 매달 항목 실행 | `1` / `2` |
| `cm\|2026-08\|m3` | 매달 항목 확인 | `1` |
| `o\|2026-08-17` | 담당자 (주 첫날 날짜) | 이름 |
| `lk\|2026-08-17` | 주차 정산 마감 | `1` |
| `cyc\|2026-08-17` | 사이클 등록 | `1` |

항목 단위로 병합되므로 두 사람이 동시에 눌러도 서로의 기록을 덮어쓰지 않는다.

시트에서 직접 고쳐도 앱이 그대로 읽는다. 다만 앱이 열려 있는 기기가 있으면
20초 뒤 동기화되니, 양쪽에서 동시에 만지지 않는 편이 낫다.

---

## 8. 알아둘 것

- 체크는 기기 브라우저에 먼저 저장되고 시트로 올라간다. **오프라인에서도 동작**하고
  연결되면 대기분이 자동 전송된다. 헤더 배지에 `동기화됨` / `대기 3` 으로 상태가 보인다
- 폰에서 **홈 화면에 추가**하면 앱처럼 쓸 수 있다
- 링크를 아는 사람은 누구나 열 수 있다. 링크는 둘만 가지고 있는 것이 좋다
